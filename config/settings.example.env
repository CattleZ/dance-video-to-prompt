# 复制为 config/settings.env
# 注意：仅 API 模式需要；Skill/Agent 模式不读这些也能工作

# Anthropic 兼容网关
ANTHROPIC_BASE_URL=https://your-gateway.example.com
ANTHROPIC_AUTH_TOKEN=your_token

# 必须是支持看图的多模态模型（API 模式）
VISION_MODEL=your-vision-model-name

# 抽帧（两种模式共用）
FRAME_INTERVAL=0.33
MAX_FRAMES=36
